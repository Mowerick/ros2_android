# Python dependencies for mock_esp32_node.py
# All packages must be available via the active ROS 2 Humble environment.
# Source ROS 2 before running: source /opt/ros/humble/setup.bash
#
# rclpy and the message packages below are installed with ROS 2 Humble -
# no pip install needed when running inside the ROS environment.
#
# rclpy                          # ROS 2 Python client library
# vermin_collector_ros_msgs      # Command / Feedback message types
# sensor_msgs                    # Imu (used by the IMU publisher, not the mock)
# geometry_msgs                  # Point (used by the target publisher)
